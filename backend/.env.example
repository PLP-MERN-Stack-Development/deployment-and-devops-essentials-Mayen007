# Example backend environment variables
# Rename to `.env` and replace values with your secrets (never commit real secrets)
PORT=5000
MONGODB_URI=mongodb://localhost:27017/mydb
MONGODB_DB=mydb
JWT_SECRET=replace_this_with_a_secret
# Optional: monitoring and logging
SENTRY_DSN=https://3cbc6200a316a4c7d5b641296b624c23@o4510374841745408.ingest.de.sentry.io/4510375329792080
LOG_LEVEL=info

# Recommended Mongoose options can be set in code; example values:
# MONGODB_MAX_POOL_SIZE=10
# MONGODB_SERVER_SELECTION_TIMEOUT_MS=5000

# Custom domain for the backend
CUSTOM_DOMAIN=https://your-custom-domain.com

# SSL/TLS certificate paths
SSL_KEY_PATH=path/to/ssl/key.pem
SSL_CERT_PATH=path/to/ssl/cert.pem
